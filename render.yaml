# Render blueprint: backend API only (frontend deployed separately)
services:
  - type: web
    name: brander-agent
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
