# Render blueprint: one web service built from Dockerfile (backend on $PORT, optional static in ./static)
services:
  - type: web
    name: brander-agent
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    # Optional: override start command to use Render's PORT (Dockerfile already uses ${PORT})
    # dockerCommand: leave unset to use Dockerfile CMD
